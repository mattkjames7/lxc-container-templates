
# debian builder
services:
  debian-builder:
    image: debian-builder:latest
    build:
      context: .
      dockerfile: Dockerfile.deb
    container_name: debian-builder
    volumes:
      - ./build:/build
      - ./output:/tmp/output
      - ./scripts:/tmp/scripts
